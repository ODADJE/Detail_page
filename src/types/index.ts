export interface Product {
  id?: string;
  enterprise: string;
  productName: string;
  desc: string;
  promotionPrice: string;
  reduction: string;
  realPrice: string;
  displaybButton: boolean;
  images: string[];
}
